"# Caesar Cipher

A simple Caesar cipher implementation in Python.

## Usage
Run \`caesar.py\` and follow the prompts to encrypt or decrypt a message.

## Notes
- Works with alphabet characters only (preserves case)
- Shift must be between 1 and 25"
